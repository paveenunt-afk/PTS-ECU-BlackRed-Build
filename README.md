# PTS ECU BlackRed v10 Build

Automated Windows installer build repository.

Windows startup hardening: isolate the bundled Qt runtime and use software
OpenGL to prevent native `0xC0000005` crashes caused by incompatible drivers.
